

{{#each posts as |post|}}
<div class="row mb-4 post">
  <div class="col-md-5">
    <h2>
      <a href="/post/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
      {{post.post_body}}
    </p>
    <p>created by {{post.user_id}} on {{post.post_date}}</p>
  </div>
</div>
{{/each}}

